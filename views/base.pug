doctype html
html
    head
        block head
            meta(charset='UTF-8')
            meta(name='viewport' content='width=device-width, initial-scale=1.0')
            link(rel='stylesheet' href='/css/style.css') 
            //- All Static files will be served from Public folder. Check out the static middleware from app.js
            link(rel='shortcut icon' type='image/png' href='/img/favicon.png')
            link(rel='stylesheet', href='https://fonts.googleapis.com/css?family=Lato:300,300i,700')
            title Natours | #{title} 
            //-Base template has access to the local variables passed into the template from app.js. 
            //- By extending the block, the contents of the block will disappear. But there is another way we can extend, where we simply add the content to the end or beginning of the block. 

    body 
        // HEADER
        include _header 

        // CONTENT
        block content
            h1 This is a placeholder heading

        // FOOTER
        include _footer

        
        //-script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/0.18.0/axios.min.js')
        //-script(src='/js/mapbox.js')
        script(src='https://js.stripe.com/v3/') 
        script(src='/js/bundle.js')



            


    



    





